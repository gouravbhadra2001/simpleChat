{
  "name": "chat_server",
  "version": "1.0.0",
  "description": "This project features a straightforward chat server that utilizes WebSockets to construct the backend of a chat interface. The client-side UI is developed using HTML and JavaScript, which renders the HTML page functional through JavaScript logic.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "author": "Gourav Bhadra",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "nodemon": "^3.1.4",
    "socket-io": "^1.0.0",
    "socket.io": "^4.7.5"
  }
}
